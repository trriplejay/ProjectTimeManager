# Language setting
language: java

# Version number
jdk:
  - openjdk11

build:
  ci:
  - mvn clean install
  - ls -l codebase/ptm_backend/target
  - ls -l codebase/ptm_cli/target
  - ls -l codebase/datamanager/target
  - mvn verify -f pom-jacocomerge.xml
  - ls -l shippable/codecoverage
